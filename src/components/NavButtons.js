import React, { Component } from 'react';


class NavButtons extends Component {


 // navigates between question pages adding or subtracting one from the route's questionNumber param

 // on first page have no back button
 // on last question change next button to submit answers which makes confirmSubmit pop up visible


  render() {
    return (
      <div>
        NavButtons
      </div>
    );
  }
}

export default NavButtons;