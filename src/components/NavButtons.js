import React, { Component } from 'react';


class NavButtons extends Component {

 // uses action creator to send code to server to be tested and get results
 // also navigates between question pages adding or subtracting one from the route's questionNumber param

  render() {
    return (
      <div>
        NavButtons
      </div>
    );
  }
}

export default NavButtons;